#include "ColorSchemeConstants.h"

namespace ColorSchemeConstants{
     //color scheme for the program / main menu
     const sf::Color TEXT_COLOR(224, 226, 219); // soft white
     const sf::Color BACKGROUND_COLOR(46, 53, 50); // soft black -- slightly green
     const sf::Color PRIMARY_COLOR(139, 38, 53); // dark red
     const sf::Color SECONDARY_COLOR( 105, 143, 63); // sage green
     const sf::Color ACCENT_COLOR( 240, 162, 2); // light sienna
}